<Window x:Class="SafeSeal.App.Dialogs.NicknameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Height="300"
        Width="660"
        MinHeight="300"
        MinWidth="620"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        Background="#F3F4F6"
        ShowInTaskbar="False">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            CornerRadius="10"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="DialogActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="102" />
            <Setter Property="Height" Value="38" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogCancelButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogActionButtonStyle}">
            <Setter Property="Background" Value="#EEF2F7" />
            <Setter Property="Foreground" Value="#374151" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DialogSaveButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogActionButtonStyle}">
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B82F6" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1D4ED8" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Border Background="White"
                CornerRadius="10"
                Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" Opacity="0.12" ShadowDepth="2" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#F8FAFC"
                        CornerRadius="10,10,0,0"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <DockPanel Margin="14,9,14,9">
                        <TextBlock Text="{Binding DialogTitle, RelativeSource={RelativeSource AncestorType=Window}}"
                                   VerticalAlignment="Center"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#1F2937" />
                    </DockPanel>
                </Border>

                <Grid Grid.Row="1" Margin="24,18,24,8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="{Binding DialogTitle, RelativeSource={RelativeSource AncestorType=Window}}"
                               FontSize="22"
                               FontWeight="Normal"
                               Foreground="#111827"
                               Margin="0,0,0,12" />

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="{Binding InputLabel, RelativeSource={RelativeSource AncestorType=Window}}"
                                   FontWeight="Bold"
                                   Margin="0,0,0,6"
                                   Foreground="#374151" />
                        <TextBox x:Name="NicknameTextBox"
                                 Height="44"
                                 MinWidth="560"
                                 HorizontalAlignment="Stretch"
                                 Padding="14,0"
                                 VerticalContentAlignment="Center"
                                 FontSize="14"
                                 BorderThickness="1"
                                 BorderBrush="#CBD5E1" />
                    </StackPanel>
                </Grid>

                <Border Grid.Row="2" Padding="24,10,24,18" Background="White" CornerRadius="0,0,10,10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel"
                                Margin="0,0,10,0"
                                IsCancel="True"
                                Style="{StaticResource DialogCancelButtonStyle}" />
                        <Button Content="Save"
                                IsDefault="True"
                                Click="Save_Click"
                                Style="{StaticResource DialogSaveButtonStyle}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>